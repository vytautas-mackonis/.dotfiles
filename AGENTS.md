# Contribution guidance

- Develop every new dotfiles feature with desired-state convergence in mind: repeated runs of `./install.sh` should move the system toward the repository-defined state regardless of its starting state.
